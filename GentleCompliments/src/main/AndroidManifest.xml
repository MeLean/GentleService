<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meline.gentleservice" >

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature
        android:name="there.isnt.a.vibrate.feature"
        android:required="false" />

    <!-- Include required permissions for Google Mobile Ads to run-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS"/>
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!--This meta-data tag is required to use Google Play Services.-->
        <meta-data android:name="com.google.android.gms.version" android:value="@integer/google_play_services_version"/>

        <receiver
            android:name=".api.api_module.GentleIntentReceiver"
            android:process=":remote" >
            <intent-filter>
                <action android:name="com.meline.gentleservice.SINGLE_SHOT_ALARM" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:configChanges="orientation|locale"
            android:screenOrientation="portrait"
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:configChanges="orientation|locale"
            android:screenOrientation="portrait"
            android:name="com.meline.gentleservice.ui.activities.AddNewComplimentActivity"
            android:label="@string/title_activity_add_new_compliment"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:configChanges="orientation|locale"
            android:screenOrientation="portrait"
            android:name="com.meline.gentleservice.ui.activities.ComplimentActivity"
            android:label="@string/title_activity_compliment"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:configChanges="orientation|locale"
            android:screenOrientation="portrait"
            android:name="com.meline.gentleservice.ui.activities.LikeHatedActivity"
            android:label="@string/title_activity_like_hated"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:configChanges="orientation|locale"
            android:screenOrientation="portrait"
            android:name="com.meline.gentleservice.ui.activities.SmsActivity"
            android:label="@string/title_activity_sms_activity"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>

        <!--Include the AdActivity configChanges and theme. -->
        <activity android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />
    </application>

</manifest>
